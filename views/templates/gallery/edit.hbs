<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  {{> scripts}}
  <title>Document</title>
</head>

<body>
  {{> nav}}
  {{> header}}
  <h3>Please edit your photo</h3>

  <div id="primaryEditContent">
    <a href="/gallery/{{photo.id}}">
      <img class="featurePhoto" src="{{photo.link}}">
    </a>
    <div class="detailAttributeBox">
      <p class="detailAttributes" id="displayEditTitle">{{photo.title}}</p>
      <span class="detailAttributes">Posted by: </span>
      <a href="/user/{{photo.user_id}}" class="userLink">{{photo.users.username}}</a>
      <p class="detailAttributes">Photo by: {{photo.author}}</p>
      <p class="detailAttributes" id="detailDescription">{{photo.description}}</p>
    </div>
  </div>

  <form class="submitForm" action="/gallery/{{photo.id}}?_method=PUT" method="POST">
    <div class="formRow">
      <label for="title">Title:</label>
      <input class="editInput" type="text" value="{{photo.title}}" id="editTitle" name="title">
    </div>
    <div class="formRow">
      <label for="author">Author:</label>
      <input class="editInput" type="text" value="{{photo.author}}" id="editAuthor" name="author">
    </div>
    <div class="formRow">
      <label for="link">Link:</label>
      <input class="editInput" type="url" value="{{photo.link}}" id="editLink" name="link">
    </div>
    <div class="formRow">
      <label for="description">Description:</label>
      <input class="editInput" type="text" value="{{photo.description}}" id="editDescription" name="description">
    </div>
    <div class="button">
      <button id="submitModify" type="submit">Submit Changes</button>
    </div>
  </form>

  <form action="/gallery/{{photo.id}}?_method=DELETE" method="post">
    <div class="button">
      <button id="deleteModify" type="submit">Delete</button>
    </div>
  </form>



</body>

</html>